<%= render 'layouts/admin_nav' %>
<div class="container">
    <h1>キャンプ場新規登録</h1>
    <div class="row">

      <table class="table">

        <thead>
          <tr>
            <td>項目</td>
            <td>入力内容</td>
          </tr>
        </thead>

          <%= form_with model: @admin_camp_site, url: '/admin_camp_sites', local: true do |f| %>
            <%= render '/error_messages', model: f.object %> <!--バリデーションメッセージ -->
            <%= render 'form', f: f %>
          <tr>
            <td>画像</td>
            <th><%= f.file_field :camp_images, multiple: true %></th></tr>
          </tr>
              <td></td>
              <th><%= f.submit class: 'btn user-edit-button' %></th></tr>
          <% end %>

      </table>

    </div>

</div>
